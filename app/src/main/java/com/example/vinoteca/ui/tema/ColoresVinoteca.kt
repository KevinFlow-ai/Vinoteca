package com.example.vinoteca.ui.tema

import androidx.compose.ui.graphics.Color

// `VinoPrimario` reutiliza el color protagonista de la marca como base de los componentes destacados.
val VinoPrimario: Color = MarcaVinoteca.colorVinoPrincipal

// `VinoProfundo` ofrece una variante más densa del color principal para fondos, gradientes y contraste en cabeceras.
val VinoProfundo: Color = MarcaVinoteca.colorVinoOscuro

// `RuborSuave` añade calidez a contenedores secundarios, chips activos y estados más amables visualmente.
val RuborSuave: Color = MarcaVinoteca.colorRubor

// `CremaBase` define el fondo principal claro de la aplicación para crear una lectura cómoda y profesional.
val CremaBase: Color = MarcaVinoteca.colorCrema

// `MarfilBrillante` se reserva para superficies protagonistas y textos sobre colores intensos.
val MarfilBrillante: Color = MarcaVinoteca.colorMarfil

// `TintaPrincipal` garantiza un color de texto fuerte y consistente sobre superficies claras.
val TintaPrincipal: Color = MarcaVinoteca.colorTinta

// `BordeDelicado` crea límites suaves entre elementos sin romper la atmósfera limpia del rediseño.
val BordeDelicado: Color = MarcaVinoteca.colorBordeSuave

// `DoradoAcento` introduce un brillo contenido para indicadores y pequeñas piezas de jerarquía visual.
val DoradoAcento: Color = MarcaVinoteca.colorAcentoDorado

// `FondoOscuroVino` funciona como equivalente oscuro de la interfaz para mantener identidad de marca en modo nocturno.
val FondoOscuroVino: Color = Color(0xFF171012)

// `SuperficieOscuraVino` separa contenedores en modo oscuro con un contraste suave y elegante.
val SuperficieOscuraVino: Color = Color(0xFF23171A)

// `TextoSobreOscuro` mejora la legibilidad del contenido principal cuando la app se muestra con fondos oscuros.
val TextoSobreOscuro: Color = Color(0xFFF7ECEF)

// `ExitoSuave` sirve para indicadores positivos o estados visuales de disponibilidad sin introducir verdes demasiado saturados.
val ExitoSuave: Color = Color(0xFF6C8A5C)
